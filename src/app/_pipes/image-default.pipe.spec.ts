import { ImageDefaultPipe } from './image-default.pipe';

describe('ImageDeafaultPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
